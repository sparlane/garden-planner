# Generated by Django 6.1 on 2026-09-11 08:45

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("health", "0005_seed_garden_health_catalogs"),
    ]

    operations = [
        migrations.AddField(
            model_name="healthdiagnosis",
            name="merged_into",
            field=models.ForeignKey(
                blank=True,
                editable=False,
                help_text="The record this duplicate was merged into. It keeps its own identity so anything written against it still resolves.",
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="merged_from",
                to="health.healthdiagnosis",
            ),
        ),
        migrations.AddField(
            model_name="healthobservationtype",
            name="merged_into",
            field=models.ForeignKey(
                blank=True,
                editable=False,
                help_text="The record this duplicate was merged into. It keeps its own identity so anything written against it still resolves.",
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="merged_from",
                to="health.healthobservationtype",
            ),
        ),
        migrations.AlterField(
            model_name="healthdiagnosis",
            name="active",
            field=models.BooleanField(
                default=True,
                help_text="Retired records stay readable everywhere they are already recorded, but are no longer offered as choices.",
            ),
        ),
        migrations.AlterField(
            model_name="healthdiagnosis",
            name="code",
            field=models.CharField(
                help_text="The stable handle everything else names this setting by. It is written once; a wrong one is corrected by merging into the record that carries the right one.",
                max_length=64,
            ),
        ),
        migrations.AlterField(
            model_name="healthobservationtype",
            name="active",
            field=models.BooleanField(
                default=True,
                help_text="Retired records stay readable everywhere they are already recorded, but are no longer offered as choices.",
            ),
        ),
        migrations.AlterField(
            model_name="healthobservationtype",
            name="code",
            field=models.CharField(
                help_text="The stable handle everything else names this setting by. It is written once; a wrong one is corrected by merging into the record that carries the right one.",
                max_length=64,
            ),
        ),
    ]
