"""Deployment settings template using PostgreSQL."""


# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = False

# REQUIRED: list the hostnames this site will be served as, for example
# ["garden.example.com"]. Leaving this empty rejects all requests.
ALLOWED_HOSTS = []

# REQUIRED for production: list the public origins, including their schemes,
# for example ["https://garden.example.com"].
CSRF_TRUSTED_ORIGINS = []

LANGUAGE_CODE = "en-us"
TIME_ZONE = "UTC"

# Database
# https://docs.djangoproject.com/en/stable/ref/settings/#databases
#
# REQUIRED: fill in every value below directly, or set DB_HOST, DB_NAME,
# DB_USER, and DB_PASS before running setup-venv.sh. Empty values make an
# incomplete database configuration fail instead of using placeholder names.

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "HOST": "",
        "NAME": "",
        "USER": "",
        "PASSWORD": "",
        "OPTIONS": {
            "pool": True,
        },
    },
}

# Recommended when TLS is terminated by a trusted reverse proxy:
# SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")
# SESSION_COOKIE_SECURE = True
# CSRF_COOKIE_SECURE = True
